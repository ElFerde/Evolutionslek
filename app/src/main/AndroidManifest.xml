<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evolutionslek">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Evolution"
            android:label="@string/title_activity_evolution"
            android:parentActivityName=".Ingame"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.evolutionslek.Ingame" />
        </activity>
        <activity android:name=".Scanning" />
        <activity android:name=".EndOfTurn" />
        <activity android:name=".IsDead" />
        <activity android:name=".Eating" />
        <activity android:name=".ShowQR" />
        <activity android:name=".Ingame" />
        <activity android:name=".StartScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>